---
interface Props {
	className?: string;
	bgColor: string;
	textColor: string;
    placeHolder: string;
}
const { className, bgColor, textColor, placeHolder } = Astro.props;
---

<input class={className} placeholder={placeHolder}>
</input>

<style define:vars={{ bgColor, textColor}}>
    input {
        background-color: var(--bgColor);
        color: var(--textColor);
        border: none;
        height: 15px;
    }
	@media (max-width: 1919px) {
        input {
            border-radius: 16px;
            padding: 18px 20px;
        }
    }
    
	@media (min-width: 1920px) { 
        input {
            border-radius: 16px;
            padding: 18px 20px;
        }
    }
</style>